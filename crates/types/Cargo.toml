[package]
name = "types"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "A library for types required in the library."

[lib]
path = "src/types.rs"

[dependencies]
